const p = (i) => {
  const e = [], t = (r, c, h) => {
    if (r.type === "text" && r.value && r.value.includes("import") && r.value.includes("from") && c && Array.isArray(c.children) && typeof h == "number") {
      c.children.splice(h, 1), e.push({
        type: "paragraph",
        children: [r]
      });
      return;
    }
    if (r.children && Array.isArray(r.children)) {
      const n = [...r.children];
      for (let l = 0; l < n.length; l++)
        t(n[l], r, l);
    }
  };
  return t(i, null, null), i.type === "root" && Array.isArray(i.children) && (i.children = [...e, ...i.children]), i;
};
export {
  p as extractImports
};
//# sourceMappingURL=extractImports.js.map
