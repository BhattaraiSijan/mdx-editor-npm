import { formatCodeAsIndented as d } from "./util/format-code-as-indented.js";
import { formatHeadingAsSetext as r } from "./util/format-heading-as-setext.js";
const i = [t];
function t(o, e, p, n) {
  if (e.type === "code" && d(e, n) && (o.type === "list" || o.type === e.type && d(o, n)))
    return !1;
  if ("spread" in p && typeof p.spread == "boolean")
    return o.type === "paragraph" && // Two paragraphs.
    (o.type === e.type || e.type === "definition" || // Paragraph followed by a setext heading.
    e.type === "heading" && r(e, n)) ? void 0 : p.spread ? 1 : 0;
}
export {
  i as join
};
//# sourceMappingURL=join.js.map
