import { ReactNode } from 'react';
import { DatasetMetadata } from '../types/content';
interface ProviderProps {
    datasets?: DatasetMetadata[];
    children: ReactNode;
}
export default function Providers({ datasets, children }: ProviderProps): any;
export {};
