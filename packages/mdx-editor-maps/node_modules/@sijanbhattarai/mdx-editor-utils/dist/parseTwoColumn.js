import { wrapComponent as t } from "./wrapComponent.js";
const m = (r) => {
  const e = [];
  for (const o of r.children)
    if (o.children.some(
      (n) => n.name === "Chart" || n.name === "Map"
    ))
      for (const n of o.children)
        (n.name === "Chart" || n.name === "Map") && e.push(t(n));
    else
      e.push({
        type: "mdxJsxFlowElement",
        name: "Prose",
        attributes: [],
        children: [...o.children]
      });
  return e;
};
export {
  m as handleTwoColumn
};
//# sourceMappingURL=parseTwoColumn.js.map
